<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @material.title %>
</p>

<p>
  <strong>Level:</strong>
  <%= @material.level %>
</p>

<p>
  <strong>Description:</strong>
  <%= @material.description %>
</p>

<p>
  <a href="<%= @material.link %>">Download</a>
</p>

<% if @material.user_id = 1 || @material.user == current_user %>
	<%= link_to 'Edit', edit_material_path(@material) %> |
<%end%>
<%= link_to 'Back', materials_path %>
