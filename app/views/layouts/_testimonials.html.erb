<div id="testimonials-wrapper">

	<h4>Opiniones</h4>

    <% @testimonials.each do |testimonial| %>
      <h5><%= testimonial.user.name %></h5>
      <p><%= testimonial.content %></p>
      <%= link_to 'Mostrar', testimonial %>
      <%= link_to 'Editar', edit_testimonial_path(testimonial) %>
      <%= link_to 'Borrar', testimonial, method: :delete, data: { confirm: 'Are you sure?' } %>
    <% end %>

	<br>

	<div><%= link_to 'Deja tu opinión', new_testimonial_path %></div>

</div>